# 任务开发日志
- 2026-05-20 已补齐前端角色对话的延迟入场与退场动效：`dialogScenes.json` 新增 `enterDelayMs`，登录欢迎语延迟 650ms、农场成熟作物提示延迟 320ms 后再弹出，避免刚进入页面时对话立即顶出。`useCharacterDialog` 新增 `entering/visible/leaving` 生命周期与滑出等待，`CharacterDialog` 按阶段挂载样式，底部弹窗现在从屏幕最下方滑入，并在关闭、自动结束或进入下一句前先向下滑出。已通过 `npm run build --workspace @trinitywar/game-client`。
- 2026-05-20 已继续优化前端角色对话系统：`dialogScenes.json` 新增 defaults、`mode`、`steps`、`advance`、`priority`、`interrupt`、`trigger` 等配置口径，支持单场景多句顺序播放、随机抽一句、洗牌播放、点击继续、自动推进、按优先级排队和替换/忽略/排队三类打断策略。`useCharacterDialog` 已从单句弹窗升级为序列控制器，`CharacterDialog` 支持多句进度与点击继续提示，现有登录欢迎和农场成熟作物土地公提示均迁移到新版配置结构。已通过 `npm run build --workspace @trinitywar/game-client`。
- 2026-05-20 已将前端角色对话改为统一 JSON 文案池驱动：新增 `apps/game-client/src/dialog/dialogScenes.json` 作为角色/场景配置源，`dialogLibrary.ts` 仅负责类型与读取，`useCharacterDialog` 只接受场景 ID 播放并支持场景几率与冷却，避免调用点直接塞文字。对话层已改为通过 portal 挂到 `document.body`，底部满宽弹出、全局最高层级覆盖；新增农场成熟作物触发的土地公场景“点击进入农场后，如果有作物熟了。土地公出来提示一句”，并保留后续可继续扩展场景池的入口。已通过 `npm run build --workspace @trinitywar/game-client`。
- 2026-05-20 已在 `apps/game-client` 新建可复用底部对话系统：新增对话语言池与角色池、`useCharacterDialog` 控制器和 `CharacterDialogProvider` 统一渲染层，支持按场景直接弹出对话、配置角色图片/文字/停留时间/自动关闭/点击 mask 关闭/关闭按钮开关，并预设登录后首页的狐狸欢迎语“主人，欢迎你回来。”。对话框按底部弹出布局实现，左侧文字、右侧角色图，占用约 1/3 屏高，后续可继续扩展更多场景和轮换文案。已通过 `npm run build --workspace @trinitywar/game-client`。
- 2026-05-20 已补齐双测试用户验证入口与自动化验证脚本：测试前端登录页新增“测试用户1/测试用户2”按钮，分别登录 `dev-verifier-1` 与 `dev-verifier-2`；后端 `dev-login` 在开发/测试环境识别这两个账号后，会同时创建或重置为主城 Lv.10、金库资金 5000、首宠 Lv.10、兽魂/天机符可用、成熟田地可掠夺，并生成双方互相可见的 `raid_target_pool`。新增 `npm run verify:test-flow --workspace @trinitywar/game-server`，脚本通过真实 HTTP 接口登录、读取 bootstrap/scene-content、升级金库、购买兽魂、升级灵宠、更新菜田留言、深度窥视、创建掠夺订单、直接调用结算服务并留言，再用 Prisma 断言钱包、建筑、灵宠、深度窥视次数、掠夺订单/锁/结算/战报/留言均已落库。已通过 `npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/shared`、`npm run build --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-client`，并在临时 3102 后端实例上通过 `VERIFY_BASE_URL=http://127.0.0.1:3102 npm run verify:test-flow --workspace @trinitywar/game-server`；验证完成后临时实例已停止。
- 2026-05-20 TW-BE-SPIRIT-002 到 TW-BE-SPIRIT-005、TW-BE-021、TW-BE-025 已继续推进并等待人工验收：新增 Migration 009 `009_spirit_intel_resource`，在 `player_spirit_resource` 中补齐天机符库存、每日领取日期、深度窥视免费/天机符次数和日期键；正式 `POST /api/client/actions/claim-tianji-talisman` 已落地，`GET /api/client/bootstrap` 返回真实天机符库存与今日领取状态，灵宠快速恢复现在真实消耗天机符并保留每日 3 次限制。掠夺深度窥视接口改为每日免费 3 次，之后每日最多消耗 3 张天机符，次数和消耗均持久化。掠夺订单创建会写入攻守双方主战灵宠快照，settlement rule 改为按灵宠等级、稀有度、阵营加成、五行克制、当前血量计算七档结果，并写入兽魂奖励、可选精魄、双方扣血和七档战报文案；BullMQ 入队在本地 Redis 缺失时改为快速失败，保留 sweep 可补结算。`apps/game-admin` 已从占位页改为只读排查前端，接入 overview、system status、玩家搜索/详情、raid order detail，不新增任何写操作。已通过 `npm run build --workspace @trinitywar/shared`、`npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-client`、`npm run build --workspace @trinitywar/game-admin`；本地 3101 smoke 验证了天机符库存、留言板、深度窥视次数、后台只读接口，并执行 `npm run worker:raid-settlement:sweep --workspace @trinitywar/game-server` 确认 `settled=2/failed=0`，最新战报显示“相持 · 平分秋色”。排期状态已更新为 `[~]`。
- 2026-05-21 已备份当天本地 PostgreSQL 数据库：从 `services/game-server/.env` 读取 `DATABASE_URL=postgresql://trinitywar:trinitywar@localhost:5432/trinitywar?schema=public`，去掉 Prisma 查询参数后使用 PostgreSQL 18 `pg_dump --format=custom` 导出到 `backups/trinitywar-2026-05-21.backup`。备份文件大小 157735 字节，`pg_restore --list` 可正常读取，清单包含 `player`、`player_wallet`、`player_daily_task_state`、`player_spirit_resource`、`raid_order`、`seed_definition` 等关键数据。明天恢复可使用：`pg_restore --clean --if-exists --no-owner --no-privileges --dbname="postgresql://trinitywar:trinitywar@localhost:5432/trinitywar" backups/trinitywar-2026-05-21.backup`。

- 2026-05-20 已按当天备份恢复本地数据库：确认 `backups/pre-restore-20260520-105211.backup` 只是恢复前自动生成的临时安全快照，已删除；随后使用 `backups/trinitywar-2026-05-20.backup` 通过 `pg_restore --clean --if-exists --no-owner --no-privileges` 恢复到 2026-05-20 口径。恢复后核对 `_prisma_migrations`、灵宠与掠夺目标数据，确认当前数据库已回到 2026-05-20 的结构与内容基线。
- 2026-05-20 TW-BE-024 已继续推进并通过本地构建/烟测：`packages/shared` 补齐掠夺灵宠预览与深度情报 DTO，`services/game-server` 的掠夺目标详情与深度窥视改为读取目标玩家真实主宠与情报，`apps/game-client` 的掠夺目标卡、目标详情、深度窥视与战报附言改为消费真实后端字段，不再依赖按 `targetId` 写死的本地表。已通过 `npm run build --workspace @trinitywar/shared`、`npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-client`，并对 `localhost:3000` 完成掠夺目标列表、详情、深度窥视接口烟测。排期状态已更新为 `[~]`，等待进一步联调验收。
- 2026-05-20 已执行数据库状态核对与备份：使用本机 PostgreSQL 18 工具检查 `_prisma_migrations`，确认数据库最近落库的结构变更仍为 2026-05-19 的 `007_spirit_foundation` 与 `008_social_message_foundation`，今天没有新增 migration 或新的 schema 落库记录。随后基于 `services/game-server/.env` 中的本地 `DATABASE_URL` 导出当天数据库备份，生成 `backups/trinitywar-2026-05-20.backup`；首次执行因 Prisma URL 自带 `?schema=public` 导致 `psql/pg_dump` 不识别，去掉该查询参数后已成功重导，最终备份文件大小 136643 字节。
- 2026-05-20 TW-BE-023 已实现并等待人工验收：`packages/shared` 补齐灵宠独立读写 DTO 与 mutation 响应契约，`services/game-server/src/spirit` 新增 `SpiritModule`、`SpiritController`、`SpiritService`，提供 `GET /api/client/spirit` 与 `buy-soul`、`upgrade`、`set-main`、`recover`、`dissolve`、`compose` 六条真实命令接口，并接入 `AppModule`。测试前端 `apps/game-client/src/api.ts` 新增灵宠 API client，`apps/game-client/src/App.tsx` 改为登录后并行加载 `viewModel + spiritState`，默认路径不再用旧 `recruit-army` 驱动灵宠页；`apps/game-client/src/ui/scenes/ArmyScene.tsx` 已重构为受真实 `ClientSpiritState` 驱动的受控组件，购买兽魂、升级、设主位、恢复、解散、合成都走后端命令，本地静态灵宠/图鉴/首宠状态不再主导默认渲染。已通过 `npm run build --workspace @trinitywar/shared`、`npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-client`。本轮未补做“快速恢复消耗天机符真实库存”的持久化，当前仍沿用每日次数限制；前后端完整手动点击联调留待人工验收。排期状态更新为 `[~]`。
- 2026-05-19 TW-BE-020 已实现并等待人工验收：新增 Nest `AdminReadonlyModule`，接入当前正式 `AppModule`，提供只读 `GET /api/admin/overview`、`/api/admin/system/status`、`/api/admin/players/search`、`/api/admin/players/:playerId/overview`、`/api/admin/players/:playerId/wallet-logs`、`/building-logs`、`/field-logs`、`/orders` 与 `/api/admin/raid/orders/:orderId`。首版后台鉴权使用 `AdminReadonlyGuard`，有 `ADMIN_DEBUG_KEY` 时要求 `x-admin-debug-key`，无 key 时仅非 production 放行；接口全部只做 Prisma 查询，不包含任何后台写操作。`packages/shared` 补充 admin 只读响应类型。已通过 `npm run build:shared`、`npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-server`；本地启动 `dist/index.js` smoke 请求 `/api/admin/overview` 与 `/api/admin/system/status` 成功，当前本机 PostgreSQL 未连通，system status 返回 `database: down`，玩家数据类接口未做真实数据请求验收。排期状态更新为 `[~]`。
- 2026-05-19 TW-BE-019 已实现并等待人工验收：前端 `scene-content` 读接口默认改为真实 API，只有显式设置 `VITE_ALLOW_MOCK_READ_FALLBACK=true` 才允许回落到 mock，`VITE_FORCE_MOCK_READS=true` 仍保留为离线视觉演示开关；`loadClientViewModel()` 中的本地训练队列和农场生命周期模拟器只在显式读 mock/允许 fallback 时运行，默认不再与真实后端读模型竞争。`start-cultivation`、`faction-donate`、`claim-starter-seeds`、`claim-tianji-talisman`、`reset-demo-state`、`raid-target` 的本地命令 handler 不再在真实接口失败时自动接管，仅在 `VITE_FORCE_MOCK_COMMANDS=true` 下启用；raid target detail 读 fallback 也改为显式策略控制。同步补充 `VITE_FORCE_MOCK_COMMANDS` 类型与 README 说明。已通过 `npm run build --workspace @trinitywar/game-client`。排期状态更新为 `[~]`。
- 2026-05-19 TW-BE-018 已实现并等待人工验收：前端读链路新增按端点控制的 `fetchReadEndpoint()` 策略，`bootstrap` 与 `home-summary` 默认要求真实 API，真实接口失败时不再静默回落到 mock；仅在显式设置 `VITE_ALLOW_MOCK_READ_FALLBACK=true` 时允许这两个接口 fallback，仅在 `VITE_FORCE_MOCK_READS=true` 时强制读本地 mock 快照。`scene-content` 本轮仍保留 fallback，留给 TW-BE-019 继续收口。同步补充 `vite-env.d.ts` 环境变量类型和 game-client README 的读 mock 开关说明。已通过 `npm run build --workspace @trinitywar/game-client`。排期状态更新为 `[~]`。
- 2026-05-18 已补齐灵宠剩余缺口数值：取消五行被动与 Lv.5、Lv.10 技能节点，明确升级只提升攻防血；深度窥视收成“每日免费 3 次 + 天机符追加 3 次”，并统一只保留天机符为付费道具；同时把普通、稀有、传说精魄掉率正式收口为逐档递减的低概率方案。
- 2026-05-18 已补齐灵宠阵营加成触发约束：只有培养并出战本阵营宠物时，才可触发同阵营轻量加成；跨阵营宠物仍可培养和出战，但不享受该增益，避免阵营规则口径含混。
- 2026-05-18 已正式收口灵宠形态成本与阶段命名：不再把 40 到 50 级做成额外主体立绘，而是改成“灵胎、幼灵、化形、真形、神异觉醒、归真圆满”六段口径，其中主体立绘只做到 30 到 39 级真形期，40 级以后主要通过边框、辉光、粒子和终阶演出强化来承接满级展示，兼顾仙侠命名和美术成本控制。
- 2026-05-18 已继续压平灵宠 41 到 50 级尾段曲线：考虑到四周内若允许最深度玩家封顶，就没必要把最后几级拉出过大的断层，因此把 41 到 50 级累计需求从约 5850 颗兽魂压到约 3360 颗，总需求从约 96.31 万金币收至约 71.41 万金币，保留 50 级珍贵感，但不再让尾段看起来明显脱离赛季现实产出。
- 2026-05-18 已按最新口径重排灵宠 41 到 50 级经验曲线：不再把 50 级做成几乎不可达的摆设，而是改成“普通玩家难到、最深度玩家四周可冲顶”的封顶结构；同步把总经验与金币跨度重新收口，明确 50 级到顶后不再继续升级，并修正摘要里残留的“理论无限提升”旧口径。
- 2026-05-18 已把灵宠数值口径进一步收紧到可执行版本：正式改成 50 级封顶且 45 级后资金跨度极大，50 级只作为理论天花板；同步补出攻击型、防御型、血量型、均衡型四类成长模板系数，明确不同宠物确实会偏攻、偏防或偏血；动画表现则收成“卡片进场、短暂摩擦、激烈终撞、展示结果”的简洁结构，并把魔界传说宠从烛龙替换为更邪性的血魇，形成仙界凤、人界龙、魔界凶兽的三界顶级宠物口径。
- 2026-05-18 已按最新讨论把灵宠进一步收向“卡牌养成 + 合成选属性”方案：前台展示正式改为卡牌化，并补入 10、20、30 级三段形态变化、40 级后转卡面特效强化；同时把五行从宠物品种固定属性改成合成入栏时由玩家直接指定，不再额外引入五行灵液或灵核等新材料，并同步改写首批 18 只宠物名单与七档战报文案口径。
- 2026-05-18 已把灵宠战斗结果从四档进一步细化为七档对称结构：前后台统一采用完胜、大胜、小胜、相持、小败、大败、完败，不再保留“前台粗结果 + 后台细分结果”的双层口径；并同步重排各档的战斗评分阈值、金币/种子/兽魂收益区间与扣血区间，让镜像局、公平局、险胜局和惨败局都能有更合理的反馈。
- 2026-05-18 已补齐灵宠战斗平局口径与首批宠物完整设定：掠夺主结果从纯三档扩成“完胜、惨胜、相持、败退”四档，避免同模板同等级同状态对撞时只能靠暗随机强行判输赢，并为相持补上少量金币、种子、兽魂奖励与中档战损区间；同时一次性补全首发 18 只灵宠的正式名称、阵营归属、五行和品种简介，并明确“同阵营加成先算、五行克制后算”的统一叠加顺序，避免后续数值打架。
- 2026-05-18 已把三界差异正式接入宠物系统，并同步收敛部分结构复杂度：恢复系统从原先多层恢复收成“自然恢复 + 天机符/点券快速全额恢复”两层结构，取消金币恢复层；宠物池改成普通 9、稀有 6、传说 3，并让三只传说宠分别对应仙界、魔界、人界。新增同阵营宠物轻量加成规则：仙界偏防御、魔界偏攻击、人界偏血量，让三界世界观开始和宠物成长线统一。
- 2026-05-18 已按最新讨论修正窥视口径冲突：默认免费窥视只显示灵宠等级与品种，不再提前暴露残血或虚弱状态；深度窥视则统一展示五行属性、攻击评级、防御评级和当前状态，进一步强化“发现对面残血后再决定出手”的付费情报价值。
- 2026-05-18 已补定掠夺三档收益与扣血区间：前台主结果保持完胜、惨胜、败退三档，但后台允许继续细分子结果用于浮动结算；同步给出金币、种子、兽魂三条收益区间，以及三档战后扣血区间，正式把掠夺、养宠、恢复三条线收成可执行闭环。
- 2026-05-18 已补齐精魄与图鉴闭环：精魄满 100 后进入待合成状态，由玩家手动合成入栏；兽栏固定 5 位，满员时不能新增宠物，必须先解散腾位；同类精魄达到 100 后不再继续累计，并明确需要配套宠物图鉴记录已见、已解锁、精魄进度、待合成与曾拥有状态，整体口径与种子图鉴保持一致。
- 2026-05-18 已收口窥视与精魄命名：深度窥视后只展示五行属性、攻击评级、防御评级和当前状态，不展示精确攻防；每次挑战、再次挑战和复仇都要重新窥视。同步把高阶掉落素材统一收敛为“某某精魄”命名，不再拆羽、牙、鳞等多种素材口径，并继续上调五行强克制，允许低 3 到 5 级稳定挑战、低 5 到 10 级在正克时搏高等级目标。
- 2026-05-18 已补齐战前窥视与强克制口径：掠夺前默认只展示对方灵宠等级、品种和状态，不展示五行属性；若玩家要进一步确认属性，可在开战前消耗点券或天机符进行一次深度窥视，再决定打不打。同时把五行克制强度明确为攻击/防御双向大幅修正，允许低 1 到 3 级玩家在正克时挑战更高等级目标。
- 2026-05-18 已补齐五栏位赛季养成锚点与五行分布：明确标准玩家一个赛季应养成 1 个超强主宠加 1 个中等副宠，并反推出主 28 级、副 18 级所需兽魂与金币量级；同时为首批普通 10 种、稀有 6 种、传说 2 种宠物补上五行归属，普通宠做到五行各 2 只，便于后续克制池、掉落池和图鉴统一落地。
- 2026-05-18 已补齐宠物收集方案：统一所有宠物以 100 精魄解锁，差异改由掉率和来源稀缺度决定；新增对战掉落与主城赠送双渠道，明确传说精魄未解锁时按月保留、不清空，并补入首批普通 10 种、稀有 6 种、传说 2 种宠物名单与传说专属精魄命名。
- 2026-05-18 已定灵宠恢复系统正式口径：保留五行而不回退三克制，恢复改为自然恢复、每日免费全额恢复、金币递增全额恢复和天机符有限次数突破四层结构；首发明确不做按比例碎片恢复，也不开放天机符无限恢复，后续商业化再视需要调整。
- 2026-05-18 已补定灵宠核心数值曲线：为所有灵宠补入统一兽魂升级需求表，明确前期快、中期稳、后期陡的无限成长口径；同步增加当前血量恢复费用曲线，并将普通/稀有/传说三档差异收敛为仅影响前中后期攻击、防御、血量成长模板的后台系数，不再拆出独立升级需求。
- 2026-05-18 已继续补齐灵宠方案：明确所有灵宠共用统一兽魂升级曲线、等级理论无限提升但由经济自然限速、灵宠升级不再绑定主城；同时将掠夺收益拆成金币/种子与兽魂两段，并把偷菜改成一键式对撞动画自动结算，用户只看短动画和结果面板，不再设计田地点击环节。
- 2026-05-18 已按新讨论收缩灵宠设计文档，明确首发战斗底层只保留攻击、防御、血量三项黑盒属性，战后持续扣当前血量并支持金币快速恢复与时间恢复；同步确认首宠三选一、掠夺前可见对手灵宠等级与状态但默认不见属性、战斗补充掉落少量兽魂、偷菜与灵宠战斗合并自动结算，以及普通/稀有/传说仅先影响成长曲线的精简口径。
- 2026-05-18 已新增灵宠等级养成与五行战斗设计草案，正式提出将灵宠从人口/数量型成长切换为单宠等级经验养成，补充兽魂喂养、五行克制、固定被动、灵伤恢复和未来多宠收藏但单宠出战的首发口径，并在旧金币曲线与人口恢复文档前置标注替代说明。
- 2026-05-17 收尾记录：本地 raid 真实联调已确认单人订单硬锁、sweep 结算、防守方田地暴露资产扣减与战报/日志落库正常；当前仅剩 Redis 运行时缺口，本机 `localhost:6379` 未启动导致 `POST /api/client/actions/raid-target` 在事务提交后进入 BullMQ 入队阶段时无法完整返回，留待明天补 Redis 后继续全链路验收。已生成可带走备份：数据库 `backups/trinitywar-2026-05-17.backup`，文档目录 `backups/docs-backend-2026-05-17.zip`。
- 2026-05-17 TW-BE-017 已实现并等待人工验收：新增 `RaidSettlementRuleService` 作为 raid 主规则层，`RaidSettlementService` 统一处理订单结算事务，BullMQ `raid-settlement.worker.ts` 独立消费 `raid-settlement` 队列并在启动时补扫到期订单；另提供 `worker:raid-settlement:sweep` 一次性补扫脚本用于本地和 CI 验证。结算事务会幂等跳过已结算订单，按锁定资产和派出兵力计算胜负、战利品、战损、溢出待领取，写入 `raid_settlement`、两侧 `battle_report`、`wallet_change_log`，更新 `raid_order.status/settled_at`、进攻方 `player_army`、进攻方钱包和 `raid_asset_lock` 状态；worker 不复制规则，直接调用同一个 `RaidSettlementService`。构建脚本同步输出 `dist/raid-settlement.worker.js` 与 `dist/raid-settlement-sweep.js`。已通过 `npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-server`、`npm run build:shared`、`npm run build --workspace @trinitywar/game-client`、`npm run db:seed --workspace @trinitywar/game-server`。本地创建验证订单 `cmp9vo65o0002146lkc1xowsy` 后执行 `npm run worker:raid-settlement:sweep --workspace @trinitywar/game-server`，结果 `settled=1/failed=0`；数据库确认订单状态 `SETTLED`，`raid_settlement` 为 `WIN/lootGold=774/depositedGold=774/overflowGold=0`，生成 2 条 `battle_report`、1 条 `wallet_change_log`，资产锁状态为 `CONSUMED`。排期状态更新为 `[~]`。
- 2026-05-17 TW-BE-016 已实现并等待人工验收：新增 Nest `GET /api/client/raid-targets/:targetId` 和 `POST /api/client/actions/raid-target`，controller 只负责鉴权、参数和 `X-Idempotency-Key` 透传；新增 `RaidTargetService` 负责真实 `raid_target_pool` 详情读取和订单创建事务，事务内校验目标池有效性、`armyVersion`、可用兵力，创建 `raid_order`，冻结进攻方兵力并写入 `raid_asset_lock`，不在 controller 或命令接口内同步计算完整结算结果。新增 `RaidSettlementQueueService` 作为 BullMQ 投递入口，订单创建后按 `settleAt` 投递 `raid-settlement` job，worker 和真实 settlement 留给 TW-BE-017。`scene-content.raid.targets` 改为读取真实目标池；dev seed 会为 `dev-main-loop` 生成指向 `dev-raid-target-a` 的目标池记录；共享 DTO 补齐 raid 的 `armyVersion/requestIdempotencyKey/orderId/settlementStatus/settleAt` 最小字段。已通过 `npm run build:shared`、`npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-client`。本轮未实现 raid settlement、battle report 最终写入和 worker 消费逻辑；这些保留给 TW-BE-017。排期状态更新为 `[~]`。
- 2026-05-17 TW-BE-015 已实现并等待人工验收：落地 Migration 006 `006_raid_order_foundation`，新增 `RaidOrderMode/RaidOrderStatus/RaidAssetLockMode/RaidAssetLockStatus/RaidSettlementResult/BattleReportType` 六个枚举，以及 `raid_target_pool`、`raid_order`、`raid_asset_lock`、`raid_settlement`、`battle_report` 五张表。`raid_order` 支持单人抢夺与通缉订单状态机、`request_idempotency_key` 唯一约束、`settle_at/status` 待结算索引；`raid_asset_lock` 单独表达软锁/硬锁、锁定资产和释放/消费/过期状态，避免把异步锁定语义写成纯 JSON；`raid_settlement` 与 `battle_report` 分离保存结算结果和双方战报。新增 `RaidModule/RaidRepository` 作为第 16/17 步复用的基础读写入口，未挂 HTTP 路由，未实现同步结算黑盒。已通过 `npx prisma validate --schema services/game-server/prisma/schema.prisma`、`npx prisma generate --schema services/game-server/prisma/schema.prisma`、`npx prisma migrate deploy --schema services/game-server/prisma/schema.prisma`、`npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-server`、`npm run build:shared`；本地数据库确认 006 migration finished，五张 raid 表和六个 enum 均存在。当前 `npm run prisma:validate --workspace @trinitywar/game-server` 在本机仍只返回 npm 生命周期失败且不透出 Prisma 详情，但等价底层 `npx prisma validate` 已通过；排期状态更新为 `[~]`。
- 2026-05-17 TW-BE-014 已完成终验收尾：复核第 14 步三条真实命令链路和既有验证记录后，补齐旧内存态 fallback 的 `armyVersion` 初始化，并让 `resetDemoState()` 同步恢复 `buildingVersion/walletVersion/armyVersion`，避免第 14 步新增的版本契约在调试态缺字段或重置后漂移。重新通过 `npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-server`，并确认本轮 `npm run build:shared` 与 `npm run build --workspace @trinitywar/game-client` 也通过。排期状态更新为 `[x]`，TW-BE-014 验收清单全部勾选。
- 2026-05-17 TW-BE-014 已实现并等待人工验收：新增正式 `POST /api/client/actions/claim-pending`、`POST /api/client/actions/claim-daily-task`、`POST /api/client/actions/recruit-army` 三条真实命令链路，controller 仅负责鉴权、请求体和 `X-Idempotency-Key` 透传，业务编排统一收口到 `ClientCommandService`，并复用既有 `AuditService`、`IdempotencyService`、`ClientReadService`。共享契约补齐 `walletVersion/armyVersion/requestIdempotencyKey`，`home-summary.stateVersions` 新增 `armyVersion`；前端首页三条高频写操作现全部透传版本号和幂等键，并移除这三条写命令的静默 mock fallback。后端新增训练队列生命周期结算服务，在 `home-summary`、`scene-content` 和 `recruit-army` 事务前按服务器时间自动结算已到期训练队列，保证 `armyVersion` 与首页展示一致。命令事务内分别完成 pending source 清空与钱包入账、任务领奖与 `task_reward_log` 写入、训练队列创建与金币扣减，并在事务中落地 `idempotency_record` 完成快照；过程中发现并修复了 `claim-daily-task` 初版误用 Prisma 关系名 `dailyTaskStates` 导致的 500，改为正确的 `taskStates` 后重新通过验证。已通过 shared build、后端 build、前端 build，并在本地 3112 实例使用 `task14-validation` 验证：`claim-pending(tax)` 成功入账 250 金币且同 key 重放返回一致快照，不同 key 重复领取返回 409 `NO_PENDING_CLAIM`；`claim-daily-task(daily-harvest-once)` 成功入账 18 金币且同 key 重放返回一致快照，不同 key 重复领取返回 409 `TASK_ALREADY_CLAIMED`；`recruit-army(5)` 成功创建训练队列，`walletVersion 5 -> 6`、`armyVersion 2 -> 3`、scene-content 中训练队列为 `queuedUnits=5/totalCost=500`，同 key 重放返回一致快照，旧 `walletVersion/armyVersion` 配新 key 返回 409 `STATE_VERSION_CONFLICT`。数据库中 `task14-claim-pending-002`、`task14-claim-task-002`、`task14-recruit-army-002` 三条 `idempotency_record` 均为 `completed`，对应 `wallet_change_log` 分别记录 `+250/+18/-500`，并新增 1 条 `task_reward_log` 与 1 条 `army_training_queue` 记录；排期状态更新为 `[~]`。
- 2026-05-17 TW-BE-013 已实现第一阶段并等待人工验收：新增正式 `POST /api/client/actions/collect-field` 命令链路，controller 仅负责鉴权、请求体和 `X-Idempotency-Key` 透传；新增田地生命周期服务，在 `home-summary`、`scene-content` 和 `collect-field` 事务中统一按服务器时间推进田地状态并递增 `fieldVersion`；`scene-content.farm.fields` 现返回 `fieldVersion`，`ClientCollectFieldRequest` 补齐 `fieldVersion/walletVersion`，前端 collect-field 主链路已透传版本号和幂等键并关闭静默 mock fallback。收取规则集中在 `FieldCommandRuleService`：仅允许成熟/枯萎正常收取和成长期提前收取，事务内完成田地清空、金库入账、`wallet_change_log`、`field_harvest_log` 和 `idempotency_record` 写入，响应继续返回刷新后的 `home` 和 `scenes`。已通过 shared build、后端 typecheck/build、前端 build，并在本地 3111 实例使用 `dev-main-loop` 验证：scene-content 中田地状态已根据服务器时间推进为枯萎期且返回 `fieldVersion`；对田地 01 使用 `ripe` 收取成功后金币 +102、`walletVersion 6 -> 7`、`fieldVersion 4 -> 5`、田地状态切回 `empty`；相同 idempotency key 重放返回一致响应；旧 `fieldVersion` 配新 key 返回 409 `STATE_VERSION_CONFLICT`；数据库中该 key 的 `idempotency_record` 为 `completed`，`wallet_change_log` 1 条、近 10 分钟 `field_harvest_log` 1 条。本轮未扩展 `start-cultivation`、任务奖励、训练或 raid 写链路，保持在农场收取闭环内，排期状态更新为 `[~]`。
- 2026-05-16 TW-BE-012 已补齐上一轮人工复核发现的两处缺口并继续等待人工验收：后端将 `upgrade-building` 的 mutation 响应快照改为在同一事务内复用 `ClientReadService` 读取并写入 `idempotency_record.response_snapshot_json`，消除“业务已提交但幂等结果未完成”的窗口；`home-summary` 现返回 `stateVersions.buildingVersion/walletVersion` 供前端真实命令链路使用；前端升级动作已透传 `buildingVersion`、`walletVersion` 与 `X-Idempotency-Key`，并移除 `upgrade-building` 的静默 mock fallback，真实接口失败会直接报错而非假成功。已重新通过 shared build、后端 typecheck/build、前端 build，并在本地 3110 实例使用 `dev-raid-target-a` 验证：`home-summary` 可返回版本号，`upgrade-building(population)` 成功后版本由 3/3 递增到 4/4、金币由 3200 降到 2500；同一 idempotency key 重放返回相同 summary 与相同版本/资源结果；旧版本号配新 key 返回 409 `STATE_VERSION_CONFLICT`；数据库中该 key 对应 `idempotency_record` 为 `completed`，`wallet_change_log` 与 `building_upgrade_log` 各 1 条。排期状态维持 `[~]`，等待人工终验。
- 2026-05-16 TW-BE-012 已实现并等待人工验收：新增 Nest `POST /api/client/actions/upgrade-building` 真实命令链路，controller 仅负责鉴权上下文、请求体和 `X-Idempotency-Key` 透传，业务编排集中在 `ClientCommandService`，升级规则集中在 `BuildingUpgradeRuleService`；命令在事务内完成建筑状态更新、金币扣减、版本递增、主城/人口派生状态更新、`wallet_change_log` 与 `building_upgrade_log` 写入，事务提交后复用 `ClientReadService` 刷新 `home-summary` 与 `scene-content`；幂等记录支持同 key 重放已完成响应，不重复扣钱，不同请求 hash 冲突返回 409。同步补齐 `ClientReadModule` 导出以供命令链路复用读服务。已通过后端 typecheck 与 build；本地 3109 验证实例中使用 `dev-main-loop` 登录后成功升级 vault（审计记录 `tw-be-012-20260516224407514`：vault Lv.4 -> Lv.5，金币 1800 -> 1150），重复同 idempotency key 后数据库仍为 `vault_level=5/building_version=4/vault_gold=1150/balance_version=4` 且升级日志/钱包日志各 1 条，使用旧版本号配新 key 返回 409 `STATE_VERSION_CONFLICT`；排期状态更新为 `[~]`。未推进 TW-BE-013 及之后命令，collect-field、recruit、claim 等写链路仍待后续任务接通。
- 2026-05-16 TW-BE-011 已实现并等待人工验收：落地 Migration 005 `005_audit_idempotency`，新增 `wallet_change_log`、`building_upgrade_log`、`field_harvest_log`、`faction_contribution_log`、`task_reward_log`、`idempotency_record` 六张核心审计/幂等支撑表，包含文档要求的外键、`playerId + endpointKey + idempotencyKey` 唯一约束及关键索引；新增最小 `AuditModule/AuditService` 与 `IdempotencyModule/IdempotencyService` 作为后续命令事务内复用入口。未实现 TW-BE-012 及之后的具体写命令。已通过 `prisma generate`、schema validate、后端 typecheck、后端 build，并执行 `prisma migrate deploy` 将 005 应用到本地数据库，查询确认六张表与 `_prisma_migrations=005_audit_idempotency` 已存在；排期状态更新为 `[~]`。
- 2026-05-16 TW-BE-010 已实现并等待人工验收：前端 `loadClientViewModel()` 已恢复并行请求 `bootstrap`、`home-summary`、`scene-content`，保留统一 fallback，但为三个读接口分别暴露 `sources.bootstrap/home/scenes`、`source` 与 `fallbackReason`；运行状态面板新增 bootstrap/home/scene 来源行，可区分全真、全 mock 与半真半假状态。未删除现有 fallback，未改后端读接口业务逻辑。已通过 `npm run build --workspace @trinitywar/game-client`，并从 `apps/game-client` 临时启动 Vite 到 5175 验证 `/index.html` 可访问；排期状态更新为 `[~]`。
- 2026-05-16 TW-BE-009 已实现并等待人工验收：在 Nest `client-read` 模块补齐 `GET /api/client/scene-content` 真实数据库读链路，新增 scene-content assembler，并扩展 repository/service/controller/module，按 player wallet、building、army/training queue、field/seed、faction/faction member/faction 榜单聚合返回 `ClientSceneContentResponse`。building、farm、army、faction 主要展示字段已来自真实库；raid/report 因当前阶段缺少 Migration 006 支撑，仅返回空列表和显式占位文案。已通过后端 typecheck、build，并使用 `dev-main-loop` 与 `dev-newbie` 登录 token 请求 `scene-content`，确认主城、田地、作物和阵营字段存在明显差异；排期状态更新为 `[~]`。
- 2026-05-16 TW-BE-007 与 TW-BE-008 已完成收尾并按当前阶段口径通过：补齐 game-server `.env` 运行时加载与 `db:seed` 入口环境加载，修复 dev 账号 seed 在重复执行时可能残留旧 faction_member 的幂等问题，统一每日任务 dateKey 为 Asia/Shanghai 本地日期口径，并把 `GET /api/client/home-summary` 的任务标题改成用户可读文案；已重新通过后端构建、`db:seed`、主循环测试号 `bootstrap`/`home-summary` 真实接口验证，排期状态更新为 `[x]`。`bootstrap` 中 `starterSeedClaimed`、`tianjiTalismanClaimed` 与全局物品仍沿当前 schema 占位默认值返回，等待后续命令持久化阶段接管，不影响 1 到 8 阶段收尾。
- 2026-05-16 TW-BE-008 已实现并等待人工验收：新增 Nest `GET /api/client/home-summary` 真实数据库读链路，按 player wallet、building、army、field、daily task 聚合返回 `HomeSummaryResponse`；已通过后端类型检查、构建，并使用开发登录 token 手动请求接口验证响应字段可返回，排期状态更新为 `[~]`。

- 2026-05-15 TW-BE-007 已落地 bootstrap 真实读接口：新增 Nest ClientReadModule/ClientReadController/ClientReadService，并在 AppModule 注册 `GET /api/client/bootstrap`；接口通过 Bearer token 读取当前玩家，从数据库聚合 SeedDefinition 与 PlayerSeedInventory，返回共享 ClientBootstrapResponse 契约。已通过 typecheck、build、本地服务启动、dev-login 后请求 bootstrap、未带 token 返回 401 验证；当前 `starterSeedClaimed` 与 `tianjiTalismanClaimed` 仍按现有 schema 默认 false，等待人工验收。
- 2026-05-15 TW-BE-006 已按自动验收规则通过：开发登录、首次建档、重复登录不重复建档、Bearer token 和 `GET /api/client/me` 均已由本地接口与数据库查询验证，排期状态更新为 `[x]`。
- 2026-05-15 TW-BE-006 已落地开发环境登录、建档与当前玩家接口：新增 Nest AuthController/AuthService/AuthTokenService、Bearer token guard、PlayerController/PlayerService 和 `GET /api/client/me`；复用 PlayerInitializationService 完成首次 dev-login 建档。已通过 typecheck、build、health、连续 dev-login 同账号不重复建档、Bearer token 请求 `/api/client/me`、新 dev 账号首次登录生成 wallet/building/army/field/inventory/task 状态验证，并确认 Swagger JSON 包含 dev-login 与 me 路径，等待人工验收。
- 2026-05-15 TW-BE-005 已完成人工验收：已在 pgAdmin4 中执行验证 SQL 并确认 faction、seed definition、DEV_FAKE 测试账号和玩家初始状态数据可查询，排期状态更新为 `[x]`。
- 2026-05-15 TW-BE-005 已落地可重复执行的 Prisma seed 与开发测试账号初始化：新增 faction/13 个 SeedDefinition 主数据、PlayerInitializationService、4 类 DEV_FAKE 测试账号和 `db:seed` 命令；已执行 prisma generate、prisma validate、typecheck、build、seed 连续两次和数据库查询验证，确认 faction=3、seed_definition=13、DEV_FAKE identity=4、field=16、inventory=52、task=12，等待人工验收。
- 2026-05-15 TW-BE-004 已完成人工验收：已在 pgAdmin 的 trinitywar/public/Tables 下确认新增核心当前状态、农场、种子和任务表可见，排期状态更新为 `[x]`。
- 2026-05-15 TW-BE-004 已落地 Prisma Migration 003/004：新增 PlayerWallet、PlayerBuilding、PlayerArmy、ArmyTrainingQueue、SeedDefinition、PlayerSeedInventory、PlayerFieldSlot、PlayerDailyTaskState 及必要枚举、版本字段、唯一约束、外键和高频读取索引；已执行 prisma format、validate、generate、typecheck、build、migrate deploy 和 migrate diff，确认本地数据库与 schema 对齐，等待人工验收。
- 2026-05-15 已新增赛季完整规则文档，统一赛季重置保留、资产继承比例、结算奖励、种子处理和追赶反滥用边界。
- 2026-05-15 TW-BE-003 已完成本地 PostgreSQL 验证：已创建 trinitywar 开发用户与数据库，成功执行 prisma migrate deploy，确认 `_prisma_migrations` 记录 001/002 已完成，且 public schema 下存在 player、player_auth_identity、faction、faction_member 表；排期状态更新为 `[x]`。
- 2026-05-15 TW-BE-003 已落地 Prisma Migration 001/002：新增 AuthProvider、Player、PlayerAuthIdentity、Faction、FactionMember schema 与两批 migration SQL，并补充 migration lock；已通过 prisma format、prisma validate、prisma generate、migrate diff、typecheck 与 build。真实 migrate deploy 未完成，原因是本地未提供可连接的 PostgreSQL/DATABASE_URL 环境。
- 2026-05-15 TW-BE-001 与 TW-BE-002 已完成人工验收：后端健康检查、Swagger、统一日志、异常结构、构建与类型检查均已由人工确认通过，排期状态更新为 `[x]`。
- 2026-05-15 TW-BE-002 已建立共享基础设施模块：新增 config、logging、common error handling、PrismaService 懒初始化注入点、auth 占位 guard/decorator/context，并在 AppModule 与 main.ts 注册全局异常过滤器和 Pino 日志；已通过 prisma:validate、typecheck、build、健康检查和 Swagger JSON smoke 验证，等待人工验收。
- 2026-05-15 TW-BE-001 已建立正式 NestJS 后端应用骨架：新增 game-server NestJS 入口、App/System 模块、健康检查、Swagger 初始化、Prisma 基础 schema、环境变量样例和构建/校验脚本；已通过 prisma:validate、typecheck、build、健康检查和 Swagger JSON smoke 验证，等待人工验收。
- 2026-05-14 已新增纯 AI 后端连续执行排期表，按阶段拆分后端骨架、迁移、读接口、写链路、掠夺异步、mock 退场与后台排查任务，并为每项补齐前置文档、验收清单和固定 AI 执行提示模板。
- 2026-05-13 已新增建筑页升级与未解锁能力展示规范，明确当前可升级区、即将解锁区、中程预告区三层信息结构，并整理主城 1 到 20 级的建筑页预告口径。
- 2026-05-13 已将前后端种子体系完整迁移为新版 13 种命名与 ID，并删除旧版种子键、默认值、掉落池与展示文案。
- 2026-05-13 已将田地位明确为随主城里程碑自动赠送开启，并把四条主城扩展分支正式接入共享类型、玩家状态、建筑页与升级接口。
- 2026-05-13 已为阵营分红领取补齐与税收一致的金币溢出判断与确认链路。
- 2026-05-13 已在顶部赛季旁接入天机符入口与全局可复用二级弹框，并修复税收确认继续领取后待领取未清空的问题。
- 2026-05-13 已将首页待领取区改为金币/种子/天机符三行紧凑布局，并接入天机符全局物品字段、每日领取接口与前后端 mock 同步。
- 2026-05-13 已修复服务端日常任务跳转映射类型错误与未使用变量报错，恢复热更新和初始金币配置生效链路。
- 2026-05-13 已将首页税收领取改回直领加溢出提示、种子改为二级弹框确认入包，并把第四块田文案修正为主城Lv.15自动解锁且回收到进度条区域。
- 2026-05-13 已修复首页奖励确认按钮不可点击与未居中问题，并补显锁定农田的主城自动解锁提示。
- 2026-05-13 已把每日任务正式接入统一配置与服务端结算，并同步补齐首页确认收取、锁定农田自动解锁提示和农田倒计时底色增强。
- 2026-05-13 已补完主城扩展升级分支与每日任务奖励首版，并转成统一服务端详细配置，补齐主城 1 到 20 级差异税收与扩展读取函数。
- 2026-05-13 已完成金币系统首稿复审，补齐真实日收入拆分、任务与付费补贴边界、金库容量口径和第 4 周战争抽水约束。
- 2026-05-12 田地状态展示已抽成可复用组件，掠夺、复仇与首页关注列表已接入对手田地状态展示和关注切换。
- 2026-05-12 已恢复掠夺、复仇与关注列表的轻量列表样式，并将多块田地展示下沉到二级详情弹框，保持与自家农场一致的布局。
- 2026-05-12 已将关注列表迁入掠夺页 tab，移除顶部和首页关注入口，并改为阵营加玩家名的简洁列表行。
- 2026-05-12 已移除部队捐献入口与结算，阵营对比只保留资金，并为非本阵营增加转阵营占位按钮。
- 2026-05-12 已为掠夺金币溢出新增 5 分钟临时待领取，并在右下角加入倒计时浮层领取入口，被动产出逻辑保持不变。
- 2026-05-12 已将掠夺二级弹框改为顶部仅保留关闭按钮，对手名称独占醒目一行，底部用关注按钮替换分享目标按钮。
- 2026-05-12 已将农场卡片压缩为图上双 tag 与底部进度，加入动态手指和收获演出延迟弹卡，保留空地与未解锁的中心入口。
- 2026-05-12 已新增服务端数值总表并接入税收、分红、建造、培育、造兵等核心金币参数，同时让前端造兵估算改为读取接口回传数值。
- 2026-05-13 已补齐首发农场与金币曲线文档中的金币来源去向总统计、四周极限推演和新手承接规则。
- 2026-05-13 已统一主城送田与播种不扣金口径，修正文档错误项，并补充税收、分红、农场、造兵、上缴与任务金币的正式首发推荐数值表。
- 2026-05-13 已将种子正式收益与阶段时长压入统一数值配置，并替换服务端与本地 mock 中默认 520 的临时培育口径。
- 2026-05-13 已让田地按真实播种时间推进播种、成长、丰熟与枯萎阶段，并输出动态剩余倒计时，不再使用固定 demo 秒数。
- 2026-05-13 已补齐前端本地 mock 的田地生命周期推进与动态倒计时，确保离线回退时与服务端阶段口径保持一致。
- 2026-05-13 已清理农场与金币设计文档中的旧口径，将种子买价改写为供给权重参考，并统一战损通过补兵而非伤兵恢复抽水的描述。
# 任务开发日志

- 2026-05-19 TW-BE-022 已完成：修复 P7.1 复核发现的测试前端 shared 契约漂移。`apps/game-client/src/mockData.ts` 补齐 `ClientSceneContentResponse.raid.messageTemplates` 的 5 条固定回复模板，以及所有 `ClientRaidTargetDetailResponse.targetFarmBoardMessage` mock 字段；未删除或放宽 shared 中已新增的真实字段。已通过 `npm run build --workspace @trinitywar/game-client`、`npm run build:shared`、`npm run typecheck --workspace @trinitywar/game-server`。排期状态更新为 `[x]`。

- 2026-05-19 TW-BE-SOCIAL-001 已实现并等待人工验收：新增 Prisma Migration 008 `008_social_message_foundation`，落地 `player_farm_board`、`raid_message_template`、`raid_order_message`；第一版不设计表情，`raidMessage.messageEmojiId` 固定为 `null`，仅预留 5 条固定掠夺回复模板。新增 `GET /api/client/profile/farm-board`、`POST /api/client/profile/farm-board` 支持玩家编辑自己的菜田留言板，包含长度、链接/联系方式和异常字符限制以及 `farmBoardVersion` 冲突校验；新增 `POST /api/client/raid-orders/:orderId/message`，仅允许攻击方在已结算订单上选择一条预设回复，并以文本快照落库。`ClientRaidTargetDetailResponse` 增加 `targetFarmBoardMessage`，`scene-content.raid.messageTemplates` 返回 5 条模板，真实战报列表会携带 `raidMessage` 快照。seed 会初始化 5 条模板和测试账号默认菜田留言。已通过 `npx prisma validate --schema services/game-server/prisma/schema.prisma`、`npx prisma generate --schema services/game-server/prisma/schema.prisma`、`npm run build:shared`、`npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-server`、`npx prisma migrate deploy --schema services/game-server/prisma/schema.prisma`、`npm run db:seed --workspace @trinitywar/game-server`；数据库查询确认模板数量为 5，`dev-main-loop` 菜田留言存在。排期状态更新为 `[~]`。

- 2026-05-19 TW-BE-SPIRIT-001 已实现并等待人工验收：新增 Prisma Migration 007 `007_spirit_foundation`，落地 `spirit_definition`、`player_spirit_resource`、`player_spirit_slot`、`player_spirit_codex` 与 SpiritRarity/SpiritRole/SpiritElement/PlayerSpiritStatus 枚举；玩家灵宠状态改为兽魂库存、5 个兽栏位、单主战位、等级/经验/五行/血量/状态/版本号和图鉴精魄进度，未新增数量型训练队列，旧 `PlayerArmy` 暂保留供现有 raid/读链路兼容。新增首批 18 个灵宠静态 seed（普通 9、稀有 6、传说 3），PlayerInitializationService 会为测试账号初始化 1 只首宠、4 个空栏位、兽魂资源和 18 条图鉴状态。已通过 `npx prisma format --schema services/game-server/prisma/schema.prisma`、显式 `DATABASE_URL` 的 `npx prisma validate --schema services/game-server/prisma/schema.prisma`、`npx prisma generate --schema services/game-server/prisma/schema.prisma`、`npm run build:shared`、`npm run typecheck --workspace @trinitywar/game-server`、`npm run build --workspace @trinitywar/game-server`、`npx prisma migrate deploy --schema services/game-server/prisma/schema.prisma`、`npm run db:seed --workspace @trinitywar/game-server`。本地数据库查询确认 spirit_definition=18/common=9/rare=6/legendary=3，`dev-main-loop` 有 spiritSoul=0、5 个兽栏位、slot1 主宠 `canglang`、图鉴 18 条且 ownedCurrent=1。排期状态更新为 `[~]`。
